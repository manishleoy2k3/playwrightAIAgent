This folder mirrors `prompts/` but is intended to live under `.github` so agents and CI can reference canonical prompt templates.

Use these files as the single source of truth for enhancer/generator/playwright prompt templates.
